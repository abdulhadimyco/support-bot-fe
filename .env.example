# Backend API URL (used in production — dev uses vite proxy)
VITE_API_URL=https://sherlock.myco.io

# Auth base URL (used in production — dev uses vite proxy)
VITE_AUTH_BASE_URL=https://authentication.myco.io
